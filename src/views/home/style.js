import styled from "styled-components";

const HomeSwrapper = styled.div`
  .contentp-wrap{
    width: 1032px;
    margin: 0 auto;
    justify-content:space-between;

  }
  
  /* .room_list {
      display: flex;
      flex-wrap: wrap;
      margin: -8px;//RoomItem组件设置padding，
      // 造成list左右两边有多余空间，采用负margin解决 
    } */
`
export default HomeSwrapper

